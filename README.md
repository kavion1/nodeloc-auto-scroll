# NodeLoc Auto Scroll & Evidence-Grounded Replier

> **当前版本**：v21.8.0（帖子一键与自动点赞版 · 深度融合 shuorenhua 去 AI 味引擎）

NodeLoc 自动拟真漫游、成长指标看板、主帖点赞与有依据的人工确认回帖 Tampermonkey 用户脚本。

---

## 🌟 核心功能亮点

### 1. 👍 主帖一键点赞与漫游自动点赞（助力「送出的赞」达标）
- **常驻一键点赞按钮**：面板顶部快捷操作区新增 `👍 点赞` 按钮，进入帖子即可一键为楼主送出好评，点赞成功后即刻变为 `❤️ 已赞`；
- **智能点赞状态检测**：自动识别楼主帖是否已赞或是否为本人发布，避免无效重复点击；
- **漫游深度阅读自动点赞**：在「🚀 漫游」设置中开启「漫游时自动点赞主帖」开关后，系统将在真实验证深度阅读（驻留 ≥ 15 秒或滚动进度 ≥ 50%）时自动为当前主题点赞，告别机械秒赞，助力论坛成长等级中「送出的赞 (Likes Given)」指标自然达标；
- **原生 DOM + Discourse API 双重驱动**：优先点击 Discourse 前端点赞控件触发原生动效，遇到非标布局时自动调取 `/post_actions` 官方接口完成点赞。

### 2. 📈 成长指标看板（官方 JSON 直连 + 健壮防卡死 + UI精调）
- **解决读取卡死与白屏**：
  - 采用 `GM_xmlhttpRequest` 扩展高权限直连（自带 Cookie 与 CSRF Token，穿透跨域与 CSP，严格 10s 超时保护），并向下兼容原生 `fetch`；
  - 严格防范数值型/对象型等级字段解析异常，杜绝运行时报错导致的界面清空与按钮永久锁定；
  - 核心生命周期使用 `try...finally` 兜底，确保刷新状态永远可恢复。
- **UI 对齐与排版深度优化**：
  - 账号输入栏全要素 `26px/28px` 高度与边距对齐，点击保存提供「✅ 已保存」即时动态反馈；
  - 新增动效加载指示器（Spinner）与错误说明卡片，杜绝数据未返回时的容器塌陷；
  - 面板底部边框与按钮增加间距隔离，解决底部按钮被卡片圆角切割问题。
- **提供 DOM 降级备用通道**：万一接口在特殊网络或权限下返回 403，看板提供「🔍 页面DOM采集」备选，多重保险。

### 3. 💊 全能迷你悬浮胶囊（折叠即全控）
- **全屏自由拖拽**：胶囊与主面板均可直接按住拖动，松手即停靠。
- **微位移防误触判定**：内置位移阈值算法，点击空白处展开，拖拽移动时绝不误触展开。
- **右下角基准平滑缩放**：收缩与展开均以右下角为基准锚点，杜绝面板展开时突然跳屏或移出视野。
- **胶囊三合一快捷微控**：
  - `⏸ / ▶`：无需展开面板，胶囊上直接一键暂停或继续漫游；
  - `⏭`：直接跳过当前帖子，平滑加载并阅读下一篇；
  - `📈 TL`：点击直接展开并切换至成长指标看板。

### 4. 🌿 深度融合 `shuorenhua`（说人话）去 AI 味引擎
- **5 大论坛高频场景自适应感知**：
  - 🎁 **抽奖福利**：严格识别口令格式，口令帖优先输出严谨口令，无口令帖随手真诚祝愿参与；
  - ⚡ **测速技术**：围绕晚高峰、回程路由、延迟、丢包或套餐性价比客观交流，保持技术真实感；
  - 💰 **集市交易**：聚焦机器配置、剩余价值、续费成本、push 费用，简明扼要；
  - 🛠️ **排障求助**：直击关键排查方向（端口、防火墙、DNS 等），拒绝无效安慰；
  - 💬 **日常杂谈**：随和接地气，就像手机真人在论坛敲字，末尾严禁加句号。
- **根除四大 AI 反模式与套话**：
  - 禁模板套话（“感谢分享/干货满满/受益匪浅/字字珠玑”）；
  - 禁二元假对比（“与其说……不如说……”、“不是……而是……”）；
  - 禁假升华与宏大愿景（“让我们拭目以待”、“开启新篇章”、“迈向新台阶”、“里程碑”）；
  - 禁机械过渡与客服反问（“总的来说”、“综上所述”、“希望对你有帮助”、“你怎么看呢”）。

### 5. 🚀 智能拟真漫游与 Discourse 机制深度对齐
- **Discourse Timing 机制对齐**：对齐 Discourse 约 60 秒上报一次阅读时长的规则，有效积累阅读天数与时长。
- **6 分钟收益封顶强切**：单帖达到 360 秒时长天花板后自动平滑切帖，长帖无须死等滚到底部。
- **分屏/独立窗口挂机友好**：基于 Page Visibility API，独立抽出窗口或多任务分屏时绝不误暂停。
- **Discourse SPA 平滑路由**：利用论坛内部路由原生跳转，无需整页刷新。

---

## 📥 安装使用

1. 安装 [Tampermonkey](https://www.tampermonkey.net/) 扩展。
2. 安装脚本：
   - 打开本仓库中的 [`nodeloc_auto_scroll.user.js`](./nodeloc_auto_scroll.user.js) 点击安装；
   - 或在 Tampermonkey 新建脚本，复制完整代码保存。
3. 打开 [NodeLoc 论坛](https://www.nodeloc.com/)，在成长指标中确认你的用户名（如 `1751140932`），即可一键查看升级进度！

---

## 🎮 快捷键与操作

- `P`：暂停或恢复自动漫游
- `S`：跳过当前帖子并进入下一篇
- `L`：为当前帖子楼主快速点赞
- **面板控制**：
  - 顶部快捷按钮：一键暂停 (`⏸ 暂停`)、快速点赞 (`👍 点赞` / `❤️ 已赞`)、跳切下一篇 (`⏭ 下一篇`)；
  - 双轨进度条：直观展示页面滚动深度与驻留计时（及 360s 封顶状态）；
  - 右上角「一」：折叠为轻量可拖拽胶囊；
  - 胶囊态：随时通过内嵌按钮启停、跳篇或展开成长指标。

---

## ⚙️ AI 配置说明

1. 切换至「⚙️ 设置」标签页。
2. 支持直接将 NewAPI JSON 导入，或手动填入：
   - **NodeLoc 用户名**：你的论坛用户名（用于读取升级进度）
   - **Base URL**：如 `https://api.openai.com` 或第三方中转地址（需包含 `https://`）
   - **API Key**：你的 API 密钥
   - **模型名称**：推荐使用极速高性价比模型（如 `claude-haiku-4-5`、`gpt-4o-mini`）
3. 所有配置仅通过 Tampermonkey 的 `GM_setValue` 本地保存，绝不上传到任何第三方服务器。

---

## ⚠️ 免责声明

本脚本仅供前端技术研究与交流使用。请合理使用自动化功能，遵守 NodeLoc 社区规范与各模型服务商协议，请勿利用脚本批量刷帖或发送违规内容。
